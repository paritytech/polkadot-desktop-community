---
description: Scaffold or review module `$2` inside `domains/$1` per CLAUDE.md structure.
---

Create directory $2 inside `domains/$1` directory. If it's already exists - make a review of existing structure.
Directory structure should be according to domain module structure defined in CLAUDE.md.
