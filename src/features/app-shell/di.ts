import { createSlot } from '@/shared/di';

export const topBarLeadingSlot = createSlot({ name: 'topBarLeadingSlot' });
export const topBarTrailingSlot = createSlot({ name: 'topBarTrailingSlot' });
export const topBarCenterLeadingSlot = createSlot({ name: 'topBarCenterLeadingSlot' });
export const topBarCenterSlot = createSlot({ name: 'topBarCenterSlot' });
export const topBarCenterTrailingSlot = createSlot({ name: 'topBarCenterTrailingSlot' });
export const tabBarSlot = createSlot({ name: 'tabBarSlot' });

export const persistentSlot = createSlot({ name: 'persistentSlot' });
